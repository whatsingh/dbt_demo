version: 2

sources:
  - name: bike_staging
    database: bigquery-public-data
    schema: austin_bikeshare
    tables:
      - name: bikeshare_trips
      - name: bikeshare_stations

models:
  - name: stg_rider
    columns:
      - name: trip_id
        tests:
          - not_null